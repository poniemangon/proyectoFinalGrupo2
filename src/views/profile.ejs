<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Mukta:wght@400;500;700&display=swap" rel="stylesheet" />
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.2.1/css/all.min.css" rel="stylesheet" />
  <link href="/css/header_footer.css" rel="stylesheet" />
  <link href="/css/profile.css" rel="stylesheet" />
  <title>Mi Perfil</title>
</head>

<body>
  <%- include('./partials/header') %>
    <br />
    <br />
    <br />
    <br />
    <br />

    <% if (user && user.id==userFound.id) {%>
      <h1 class="profile-title">Bienvenido a tu perfil, "<%= userFound.name %>"
      </h1>
      <% } else { %>
        <h1 class="profile-title">Perfil de <%= userFound.username %>
        </h1>
        <% }%>
          <div class="profile-container">
            <% if (user && user.id==userFound.id) {%>

              <img src="/images/userimages/<%= userFound.image %>" alt="" class="profile-user-img" />
              <h2 class="profile-user-name">Nombre: <%= userFound.name %></h2>
              <h2 class="profile-user-name">Apellido: <%= userFound.surname %></h2>
              <h2 class="profile-user-name">Categoria: <%= user.category %></h2>
              </h2>
              <h2 class="profile-user-email">Email: <%= userFound.email %>
              </h2>

              <a href="/edituser/<%= user.id %>" class="profile-edit-data">Editar datos</a>
              <a href="/delete/<%= user.id %>" class="profile-delete">Eliminar usuario</a> 

              <% } else { %>

                <img src="/images/userimages/<%= userFound.image %>" alt="" class="profile-user-img" />

                <h2 class="profile-user-name">Nombre: <%= userFound.username %>
                </h2>

                <h2 class="profile-user-email">Email: <%= userFound.email %>
                </h2>

                <% }%>
                  <% if (user && user.id==userFound.id) {%>

                    <!-- <p><%= userFound.name %></p> -->
                    <a  href="/logout" class="profile-logout">Cerrar Sesión</a>

                    <%if (userFound.id_user_category == 2) { %>
                      <a href="/products/create" class="profile-crear-prod">Crear Producto</a>
                    <% } %> 

                    <% }%>
          </div>
          <%- include('./partials/footer') %>
</body>

</html>