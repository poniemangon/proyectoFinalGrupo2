<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Mukta:wght@400;500;700&display=swap"
      rel="stylesheet"
    />
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.2.1/css/all.min.css"
      rel="stylesheet"
    />
    <link href="/css/home.css" rel="stylesheet" />
    <link href="/css/header_footer.css" rel="stylesheet" />
    <link href="/css/carrito.css" rel="stylesheet" />
    <link href="/css/agregar.css" rel="stylesheet"/>
    <title>Document</title>
  </head>
  <body>
    <%- include('./partials/header') %>
    <br>
    <br>
    <br>
    <br>
    <br>
    <div>
      
      <% if (user && user.id == picUser.id) {%>
      <p>editar perfil de <%= user.username %> </p>
      <div class="formLog">

        <form method="POST" action="/edituser/<%= picUser.id %>?_method=PUT" enctype="/">
          <% if (locals.errors) { %>
            <p>
              <% errors %>
            </p>
            <% } %>
              <div>
                <label for="username">Username</label>
                <input type="text" id="username" name="username" value="<%= picUser.username %>" required>
              </div>
              <div>
                <label for="password">Password</label>
                <input type="password" id="password" name="password" required>
              </div>
              <div>
                <label for="name">Name</label>
                <input type="text" id="name" name="name" value="<%=picUser.name%>" required>
              </div>
              <div>
                <label for="mail">Email</label>
                <input type="text" id="mail" name="mail" value="<%=picUser.mail%>" required>
              </div>
    
    
              <div>
                <button type="submit">Change data</button>
              </div>
        </form>
      </div>

      <% } else { %>
        <p>no podes editar un perfil que no sea el tuyo</p>
      <% }%>
    </div>
  </body>